
### - 문제 -
- 발생 위치: [함수명/파일명]
	run_process_orders()/C:\laragon\www\ship_new_v2\app\jobs
- 내용
	step1에서 설정된 사이트 값을 가져오지 않고 쿠팡 데이터만 api를 통해 가져온다.

### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- site_settings테이블에서 is_active 값이 1인 값을 가져와 1에 해당되는 사이트에 api를
  던져서 모두 주문 데이터를 가져와야 한다.
- 현재는 step1 내에서 쿠팡에 한해서만 가져오고 is_active 값이 1인 smartstore의 값은 가져오지 않고 있다.
  그리고 smartstore 주문가져오는 api는 step1에 포함시키지 않고 따로 페이지를 만들어져있다.
  해당 함수 동일한 경로에 collect_naver_orders.php로만 만들어놨다.
- 결론 step1에 is_active 1인 값을 통해 해당 사이트 주문내역을 가져오게 하면된다.
  그리고 여기서 step1에 smartstore 주문 가져오는 api를 새로 작성하거나, collect_naver_order.php를 참조해서
  함수를 사용해서 주문데이터를 가져오게 해도 된다.

### - 체크 -
- 수정 요청한 파일, 함수에서만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 가독성 있게 띄어쓰기, 줄바꿈 하기
- PHP [버전] 호환성 준수
- 한글 깨짐 방지 (UTF-8)

==========================================================================================
### - 문제 -
- 발생 위치: [함수명/파일명]
	run_process_orders()/C:\laragon\www\ship_new_v2\app\jobs
- 내용
	step1_collect_orders 내 코드 작성이 가독성 없이 한줄로 작성됨

### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- 체크 내용을 보면 코드 작성시 가독성 있게 띄어쓰기, 줄바꿈을 하기로 작성되어 있는데
  문제의 내용처럼 한줄로 작성되어 보기가 너무 어려우므로 가독성 있게 바꿔줘.

### - 체크 -
- 수정 요청한 파일, 함수에서만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)
=============================================================
### - 문제 -
- 발생 위치: [함수명/파일명]
	run_collect_naver_orders()/ ship_new_v2 -> collect_naver_orders.php
- 내용
	NAVER_API_BASE_URL is required
	url 셋팅도 안되어 있고, api 내용이 많이 부족

### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- https://apicenter.commerce.naver.com/docs/commerce-api/current/seller-confirm-placed-product-orders-pay-order-seller
  해당 smartstore 발주확인처리 api 관련 url이야.
  위 내용 확인 후 해당 함수 다시 셋팅하자.

### - 체크 -
- 수정 요청한 파일, 함수에서만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)
=================================================================
### - 문제 -
- 발생 위치: [함수명/파일명]
	naver_collect_get_access_token()/ ship_new_v2 -> collect_naver_orders.php
- 내용
	[warn] NAVER_ACCESS_TOKEN not set and token endpoint credentials missing
	[error] Naver access token unavailable


### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- 위와 같이 에러 발생. 에러 해결할 수 있는 방법을 찾아서 필요한 정보가 있으면 요청을 하고 아니면 에러를 해결해줘

### - 체크 -
- 수정 요청한 파일, 함수에서만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

=====================================================================

### - 문제 -
- 발생 위치: [함수명/파일명]
	naver_collect_get_access_token() / ship_new_v2 -> collect_naver_orders.php
- 내용
	[warn] Naver token issue config missing: NAVER_CLIENT_ID, NAVER_CLIENT_SECRET, NAVER_ACCOUNT_ID
	[error] Naver access token unavailable


### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- 스마트스토어는 API 사용하기 전에 인증토큰을 갱신? 받아서 이후 주문 등등 API를 던져서 사용할 수 있는거
  같은데 지금은 내가 직접적으로 정보를 입력한 내용은 없다. 필요한 정보가 있으면 어느 위치에 작성해야 하는지 말해줘
  위 warn, error 부분 체크해주고 코드가 이상있으면 수정하고 아니라면 내가 말한 정보가 필요한거면 어느 위치에 작성을 해야하는지 알려줘

### - 체크 -
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

===========================================================================

### - 문제 -
- 발생 위치: [함수명/파일명]
	naver_collect_get_access_token() / ship_new_v2 -> collect_naver_orders.php
- 내용
	NAVER_TOKEN_URL=https://api.commerce.naver.com/external/v1/oauth2/token
	NAVER_API_BASE_URL=https://api.commerce.naver.com/external/v1/pay-order/seller/product-orders/confirm
	NAVER_CLIENT_ID
	NAVER_CLIENT_SECRET
	NAVER_TOKEN_TYPE=SELLER
	NAVER_ACCOUNT_ID


### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- .env 내용 작성, 비밀정보라 ID등 내용은 지우고 해당 파일에는 작성되어 있음.
  작성된 내용으로 다시 한ㅂ

### - 체크 -
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

=====================================================================

### - 문제 -
- 발생 위치: [함수명/파일명]
	step2_normalize_coupang()  / ship_new_v2 -> process_orders.php
- 내용
	orders_raw 테이블까지 coupang, smartstore 둘다 저장
	step2 -> 정규화 하는 작업에선 너가 말한대로 coupang 정규화만 존재
	smartstore 정규화 추가


### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- step2에 smartstore도 정규화 작업 추가
  smartstore_order_excel 해당 테이블에 정규화된 데이터 insert
  coupang 정규화 하는 방식으로 동일하게 테이터 양식에만 맞춰서 진행

### - 체크 -
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### - 문제 -
- 발생 위치: [함수명/파일명]
	step4_make_lozen_file()  / ship_new_v2 -> process_orders.php
- 내용
	step3는 아직 smartstore 주문이 없기 때문에 테스트를 해볼수 없어서 이 부분은 패스하고
	step4에서 2와 같이 coupang만 로젠형식에 맞춰 엑셀 다운로드 되는 것으로 보인다.


### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- step4에 smartstore도 로젠 형식에 맞춰 데이터를 가져와 엑설 작성하기
  smartstore_order_excel 해당 테이블에 정규화된 데이터를 가져와 로젠 엑셀 형식에 맞춰 작업
  coupang 로젠 엑셀 형식으로 다운로드 하는 방식으로 동일하게 테이터 양식에만 맞춰서 진행

### - 체크 -
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### - 문제 -
- 발생 위치: [함수명/파일명]
	step4_make_lozen_file()  / ship_new_v2 -> process_orders.php
- 내용
	step3는 아직 smartstore 주문이 없기 때문에 테스트를 해볼수 없어서 이 부분은 패스하고
	step4에서 2와 같이 coupang만 로젠형식에 맞춰 엑셀 다운로드 되는 것으로 보인다.


### - 요구사항 -
- [기존 기능 유지하며 에러만 수정]
- step4에 smartstore도 로젠 형식에 맞춰 데이터를 가져와 엑설 작성하기
  smartstore_order_excel 해당 테이블에 정규화된 데이터를 가져와 로젠 엑셀 형식에 맞춰 작업
  coupang 로젠 엑셀 형식으로 다운로드 하는 방식으로 동일하게 테이터 양식에만 맞춰서 진행

### - 체크 -
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)ㅍ

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	dashboard 메인페이지 오른쪽 상단 미배송 엑셀 재다운 버튼(클릭시 액션)
- 내용
	미배송 엑셀 재다운 클릭(액션) -> 여기선 아직 smartstore 데이터에 대해 가져오지 않는다.


### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 클릭(액션)시 현재는 coupang만 가져오는데 smartstore에서도 해당 데이터를 가져와서
  로젠형식에 맞춰 작성해서 엑셀 다운로드 되게 수정

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	step4_make_lozen_file() -> process_orders.php
	dashboard 메인페이지 오른쪽 상단 미배송 엑셀 재다운 버튼(클릭시 액션)
- 내용
	신규주문 수집에서의 step4에 해당하는 로젠 다운로드에서는 이전에 작업을 통해
	smartstore데이터를 가져오긴 하지만 아래의 요구사항처럼 값을 제대로 가져오지 못한다.
	추가된 smartstore 데이터를 엑셀을 보면 제대로 가져오지 못한다.

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 첨부된 이미지와 같이 coupang 데이터를 기존처럼 잘 가져온다.
  하지만 smartstore 데이터는 제대로 가져오지 못한다.
  1. A열 수취인이름이 값이 없음
  2. B열 수취인전화번호 값이 없음
  3. D열 상품명이 "SMARTSTORE, 1개" 다 동일하게 이 값으로 있음, 상품명을 제대로 가져오지 못함
  4. F열 수취인 주소 값이 없음
  5. G열 배송메세지가 없을 수도 있는데, 있는데 값이 없는것도 있음
  6. I열 상품옵션을 보고 택배사이즈 선별해서 값이 작성되어야 하는데 값이 없다.
  7. J열 주문번호를 가져오긴 하는데 coupang 데이터를 가져오는 값보다 긴데 양식이 달라
     엑셀 수정을 요할땐 그 값이 길이 때문인지 맨 뒤자리가 변경되곤 함.

  위의 문제들을 파악해서 제대로 된 값을 들어가도록 수정

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	step4_make_lozen_file() -> process_orders.php
	dashboard 메인페이지 오른쪽 상단 미배송 엑셀 재다운 버튼(클릭시 액션)
- 내용
	신규주문 수집에서의 step4에 해당하는 로젠 다운로드에서는 이전에 작업을 통해
	smartstore데이터를 가져오긴 하지만 아래의 요구사항처럼 값을 제대로 가져오지 못한다.
	추가된 smartstore 데이터를 엑셀을 보면 제대로 가져오지 못한다.

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 1. D열 상품명이 "SMARTSTORE, 1개" 다 동일하게 이 값으로 있음, 상품명을 제대로 가져오지 못함

  위의 문제들을 파악해서 제대로 된 값을 들어가도록 수정

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	step2_normalize_coupang(), step3_coupang_prepare -> process_orders.php
- 내용
	step2에선 함수명이 정규화 쿠팡인데 그 안에서는 쿠팡, 스마트스토어를 다하고 있다.
	step3에선 현재는 쿠팡만 발주요청 처리를 진행하고 있다.

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- step2 함수명을 보면 쿠팡만 작업하는걸로 느껴찜.
  1. 함수명 변경(특정 사이트가 아닌걸로 예를 들면 step2_normalize())
  2. 함수내에 쿠팡, 네이버를 나눠서 따로 함수를 정의하고 step2 새로 명한 이름내에 나눈 쿠팡, 스마트스토어 함수를 불러오기
  (함수명 정의하고 나눠져야 흐름이나 읽기 좋을꺼 같다)

  step3 함수명을 보면 쿠팡만 작업하는걸로 느낌.
  1. 함수명 변경(특정 사이트가 아닌걸로 예를 들면 step3_prepare())
  2. 현재는 쿠팡만 작업되어 있다. 이부분만 빼서 쿠팡 발주요청 함수로 생성하고 step3에서 해당 함수 불러오도록 한다.
  (함수명 정의하고 나눠져야 흐름이나 읽기 좋을꺼 같다)

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	step3_prepare -> process_orders.php
- 내용
	step3에선 쿠팡만 api를 통해 상품준비중으로 변경을 하고 있다. 

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- smartstore도 쿠팡과 같이 따로 함수를 정의해서 만들기.
  아래 참조URL를 통해 주문상품 발주확인 처리로 작업

참조 URL
https://apicenter.commerce.naver.com/docs/commerce-api/current/seller-confirm-placed-product-orders-pay-order-seller

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	파일전체 -> process_shipping.php
- 내용
	이전에 작업을 하면서 한글이 깨지고 코드 작성시 한줄느낌으로 작업

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 해당 파일 전체 한글깨진 부분 수정
  코드 줄바꿈/들어쓰기해서 가독성 있게 작업
  UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	파일전체 -> process_shipping.php
- 내용
	해당 파일 전제적인 흐름은 앞전에 신규주문 확인해서
	데이터 가져와서 정규화를 흘러 로젠 엑셀을 다운 받았다면,
	여기선 로젠 송장을 출력한 이후에 출력한 내용을 엑셀 다운받아
	서버에 업로드한 후 그 업로드된 엑셀 기준으로 쿠팡 API를 통해
	주문번호 기준으로 상태를 배송지시로 변경하는 파일

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 현재 쿠팡만 되어 있다.
  여기에 스마트스토어를 추가한다.
  추가함에 있어 기존 소스에 추가하지만 작업하는 내용을 따로 함수로 만들어서
  쿠팡, 스마트스토어 이런식으로 나눠서 만들어서 해당 스텝에 맞는 함수에서 각가 쿠팡, 스마트스토어를
  불러와 실행되게 작업(시스템의 흐름을 편하게 하면서 소스 읽기 편하게 하기 위함)

  참고URL
  https://apicenter.commerce.naver.com/docs/commerce-api/current/seller-dispatch-product-orders-pay-order-seller

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	run_process_shipping()  
	-> ensure_coupang_order_excel_delivery_columns()
	/ process_shipping.php
- 내용
	ensure_coupang_order_excel_delivery_columns() 함수는
	배송지시 상태 변경전 테이블 컬럼 체크하는 부분의 함수이다.

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 위의 함수가 쿠팡에 관련된 내용 밖에 없다.
  스마트스토어도 동일한 컬럼 체크하는 함수를 만들어서 체크하게 해줘
  그리고 run_process_shipping()에서 만든 함수를 불러 체크하게끔 작업해줘

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)


==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	step4_make_lozen_file() / process_orders.php
- 내용
	신규주문 프로세스 중 마지막인 로젠형식에 맞춰 엑셀 다운로드 구간이다.

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 어제 smartstore 신규주문 작업한 내역들이 오늘도 신규주문으로 로젠형식에 맞춰 다운로드됨
  어제 배송지시를 하면서 해당 컬럼을 업데이트를 안해줘서 그런건지
  아니면 어떤 문제인지 정확하게 파악하진 못했지만 오늘 smartstore는 신규주문이 0개이다.
  출력물을 확인하니 어제 신규주문 3개가 출력되서 나옴.

  위의 내용을 파악해서 어느 부분에서 잘못된 부분인지 파악 후 
  내가 말한대로 잘 돌아가서 수정해줘

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 미배송 엑셀 재다운 / 메인대시 화면
- 내용
	신규주문 실행이후 다시 로젠엑셀 다운 받을 수 사용하는
	버튼입니다.

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 이전 작업을 통해 smartstore에 어제 데이터도 나와 수정을 하였는데,
  미배송 엑셀 재다운 버튼 클릭시 이때도 어제 데이터가 나오지 않아야 하는데
  신규주문 아까 처음 나오는 데이터가 동일하다.

  어제 데이터는 신규주문이후 송장데이터까지 업데이트 했으므로,
  어제데이터 확인 후 다시 안나오도록 업데이트 해야함.

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 step4_make_lozen_file() / process_orders.php
- 내용
	각 사이트별 정규화한 데이터를 가져와 로젠형식에 맞춰 작성해서 엑셀 만드는 구간

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 로젠형식
  상품명, 수량, 택배비 이 부분을 옵션관리에 등록된 데이터를 비교해서
  가져와 작성하는걸로 알고 있다.

  이 부분이 어디 함수 어느쪽에서 작업이 되는지 알려줘
  어떤식으로 구분해서 선택을 하고 그런 세세한 부분까지 알려줘

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 step4_make_lozen_file() / process_orders.php
- 내용
	각 사이트별 정규화한 데이터를 가져와 로젠형식에 맞춰 작성해서 엑셀 만드는 구간

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 그럼 여기서 step4_make_lozen_file() 안에서 coupang, smartstore 한 함수에 다 작성되어 있는데,
흐름, 가독성 좋게 하기 위해 coupang 함수, smartstore 함수 만들어서 step4_make_lozen_file()에 불러서
사용하게끔 작업 해줘

그리고 coupang, smartstore안에서 크게 흐름처럼 나눠지는 구간 주석을 달아줘! 

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 step2_normalize() / process_orders.php
- 내용
	각각 사이트에서 데이터를 가져와 각 테이블에 정규화하여 insert하는 함수

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 스마트스토어도 정규화 단계에서 필드를 더 꽉 채우도록 바꿔서 step4의 orders_raw 의존도를 줄여줘
  (줄이기보단 의존 없이 각 테이블에서 해결 가능하게끔 작업해줘)

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 step4_make_lozen_file() / process_orders.php
- 내용
	각각 사이트에서 데이터를 가져와 각 테이블에 정규화 한후 해당 데이터를 가져와 로젠형식에 맞춰
	뽑아내서 엑셀을 만드는 함수

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 이전에 smartstore 로젠형식, 정규화 과정에서 잘해서 orders_raw 의존하지 않게 작업을 했는데,
  오늘 오전 신규주문 실행시 다운로드된 엑셀이 제대로 나오지 않았음
  해당 엑셀은 첨부해서 보여줄께.

  이 부분에서 내가 볼땐 정규화가 잘 안되 해당 smartstore 테이블에서 가져오는 데이터가 없거나 뭔가 맞지 않아서 안가져오는
  부분인거 같아.

  위의 내용 잘 파악해서 orders_raw 의존하지 않게 하고, 그럼 정규화쪽에서부터 정규화를 잘해야된다고 생각해.
  정규화가 잘되야 로젠 파일 생성시 smartstore 테이블만 보고 가져와서 보여줘야 한다고 생각해.

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 step4_make_lozen_file() / process_orders.php
- 내용  
	각각 사이트에서 데이터를 가져와 각 테이블에 정규화 한후 해당 데이터를 가져와 로젠형식에 맞춰
	뽑아내서 엑셀을 만드는 함수

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 방금전 보강 작업을 통해 내용을 확인하면 데이터는 가져왔음.
  문제) 1. 이전에 배송지시한 데이터를 가져오고 있음
       (첨부한 엑셀파일보면 김정규, 쥬쥬, 구재욱은 오늘 신규주문으로 가져온 데이터가 아닌 오늘 이전의 데이터다)
       2. 상품명, 수량을 제대로 가져오지 못하는것 같다.
       (옵션관리를 통해 해당되는 상품명을 가져오는데 smartstroes는 쿠팡과 조금은 다르게 작업해야한다.
        단일상품일땐 기존의 방식이 맞지 않는다. raw_json 데이터 안에 내용을 보고 1차적으론
	option_ID로 비교를 하고 없다면 상품ID로 비교를해서 맞는 상품명을 가져와야한다.)
       3. 2번에서 수량을 잘 못가져온다고 한건 조남청의 실제 주문수량은 2개이다.
  

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 step2_normalize_smartstore_orders() / process_orders.php
- 내용  
	스마트스토어 api를 통해 가져온 데이터를 정규화 작업

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 신규주문 수집하고 정규화 하는 작업에서 배송메세지가 계속 빠져있어서
  소스, db 데이터를 확인해봤는데 smartstore_order_excel 테이블에 배송메세지에 대한 컬럼이 없어보인다.

  1. 배송메지시 컬럼 없으면 만들기
  2. orders_raw 테이블 raw_json에 배송메세지 내용이 들어가는지 확인하기
  3. 2번이 잘 들어간다면, 1번에 컬럼 만든곳에 2번의 데이터를 들어가게 만들기
  

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 해당 파일에 전체 함수 / process_orders.php
- 내용  
	신규주문 버튼 클릭시 실행되는 STEP 1~4 전체 내용

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 현재 신규주문 버튼 클릭에 해당되는 시스템은 완료되었다고 본다.
  여기서 체크포인트가 있어서 너랑 대화를 하면서 어떤 방향성으로 갈지 정하고 싶다.

  최종 결과는 로젠형식에 맞게 작성된 엑셀파일이 다운로드 된다.
  해당 파일은 내가 직접 로젠 웹사이트를 통해 송장 출력을 한다.
  출력전에 엑셀파일을 열어 확인을 한번 하고 업로드에서 출력을 한다.
  여기서 엑셀파일을 열어 확인해보는게 체크포인트이다.
  확인하는 이유는 100% 확실하지 않을 수 있다는 믿음때문이다.
  그래서 쿠팡, 스마트스토어 실제 판매관리자사이트에 접속해서 한번 비교를 해본다.
  이렇게 하는 이유는 시스템 실수로 인해 발생되는 잘못된 정보로 인해
  CS, 직원이 다시 작업하는 피로도, 추가되는 배송비는 회사 비용 등이 발생하기 때문이다.
  말 그대로 첫 배송시 이상없이 잘 보내서 그 뒤의 발생되는 작업이 없길 바라는거다.

  위와 같은 체크포인트가 있어. 이 부분은 현재 시스템에서 어떻게 하는게 좋은지 너와 체크해보고
  방향성을 잡고 작업을 하고 싶어.
  

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 해당 파일에 전체 함수 / process_orders.php
- 내용  
	신규주문 버튼 클릭시 실행되는 STEP 1~4 전체 내용

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 
	1. 현재 상황 및 배경
	완료된 기능: '신규 주문' 버튼 클릭 시 데이터를 수집하는 백엔드 로직 완료.
	최종 목적: 수집된 데이터를 로젠택배 양식의 엑셀 파일로 다운로드하여 송장 출력.
	현재의 불안 요소: 시스템 오류로 인한 잘못된 정보(주소 오기입, 수량 오류 등)가 포함될 가능성.
	오류 발생 시 리스크: CS 업무 가중, 재작업 피로도, 불필요한 왕복 배송비 등 회사 비용 발생.

	2. 핵심 체크포인트 (Human-in-the-loop)
	현재 나는 엑셀을 다운로드한 후, 쿠팡 및 스마트스토어 관리자 센터에 직접 접속하여 데이터를 1:1로 대조하는 과정을 거치려 함. 
	이는 시스템이 100% 완벽하지 않을 수 있다는 전제하에 '첫 배송 사고'를 막기 위한 필수 검증 단계임.

	3. 논의하고 싶은 방향성 (질문 사항)
	이 검증 과정을 '단순 노가다'가 아닌, 시스템적으로 보완하거나 효율화할 방법에 대해 의견을 듣고 싶음
  

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 해당 파일에 전체 함수 / process_orders.php
- 내용  
	신규주문 버튼 클릭시 실행되는 STEP 1~4 전체 내용

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
-	검증 대상을 100%가 아니라 “위험 주문”으로 축소
	자동 대조용 “검증 시트”를 함께 생성
	배포 초기에는 “게이트” 운영
	오류 히스토리 기반 룰 강화

	위는 너가 제시한 방법

	1.검증용 기준 데이터 생성
		정규화 완료 시점에 검증용 summary 테이블 생성
		기준: order_no / option_id / qty / receiver
	2.엑셀 생성 결과도 동일 구조로 변환
		로젠 엑셀 생성 직후
		동일한 포맷으로 “엑셀 데이터 파싱 → temp 배열 생성”
	3.자동 비교 로직 실행
		기준 데이터 vs 엑셀 데이터 비교
		체크 항목:
			주문 수
			옵션별 수량
			총 수량
			수취인 기준 그룹핑
	4.불일치 발생 시
		엑셀 다운로드 차단
		오류 로그 + UI 표시
	5.정상일 때만
		엑셀 다운로드 허용
		lozen_exported_at 업데이트

	이 방법과 비교를 했을때 어느 방법으로 체크하는게 좋아?


### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

적용 파일
	process_orders.php

목표
	1.로젠 엑셀 생성 전에 자동 정합성 검증(차단 게이트)
	2.통과 건 중 위험 주문 자동 선별(검수 리스트)
	3.둘 다 같은 실행(신규주문 STEP4)에서 처리

함수 설계
	1.step4_make_lozen_file()
		기존 엑셀 행 생성 직후 아래 순서 호출:
		build_baseline_summary($jobId, $fromDate, $toDate)
		build_export_summary($rowsForExcel)
		compare_summaries($baseline, $export)
		불일치면 job_log(..., 'error', ...) 후 파일 생성/다운로드 중단
		일치면 build_risk_orders($rowsForExcel) 실행
		위험건을 별도 시트(또는 별도 csv/xlsx)로 같이 저장
		정상 시에만 lozen_exported_at 업데이트
	2.build_baseline_summary(...)
		기준 데이터 원천: 정규화 테이블(쿠팡/스마트스토어 각 테이블)
		공통 키 생성: platform + order_no + option_id(or product_id fallback)
		집계 항목:
		주문 건수
		옵션별 수량 합
		총 수량
		수취인+전화번호 그룹 건수
	3.build_export_summary(array $rowsForExcel)
		실제 엑셀에 들어갈 배열을 동일한 공통 키 포맷으로 재집계
		기준과 동일 항목(주문수/옵션수량/총수량/수취인그룹) 생성
	4.compare_summaries(array $baseline, array $export): array
		반환: ['ok'=>bool, 'diffs'=>[]]
		비교 실패 조건:
			주문수 다름
			옵션별 수량 다름
			총수량 다름
			수취인 그룹 다름
		diffs는 로그/화면 표시용 텍스트로 구성
	5.build_risk_orders(array $rowsForExcel): array
		위험 5규칙 적용:
			주소 위험
			연락처 위험
			상품 매핑 위험
			수량 위험
			중복/재출고 위험
		반환: 위험 주문 목록 + 위험 사유 코드
	6.write_risk_sheet_or_file(...)
		로젠 본시트는 유지
		검수대상 시트 추가(권장) 또는 별도 lozen_risk_*.xlsx 저장
		컬럼 예: 주문번호, 플랫폼, 수취인, 전화번호, 상품명, 수량, 위험사유
	7.update_lozen_exported_at_only_on_success(...)
		compare_summaries().ok === true일 때만 실행
		실패 시 업데이트 금지(재처리 가능 상태 유지)

실행 흐름 (STEP4 내부)
	1.쿠팡/스마트스토어 정규화 테이블에서 로젠 행 생성
	2.기준요약 vs 엑셀요약 자동비교
	3.실패 시 다운로드 차단 + 오류로그
	4.성공 시 위험주문 시트 생성
	5.파일 저장 + lozen_exported_at 업데이트

운영 포인트
	1.초기는 “비교 실패 시 무조건 차단”으로 강하게 운영
	2.위험주문은 100% 수동검수
	3.안정화 후 위험규칙만 점진적으로 고도화

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 해당 파일에 전체 함수 / process_orders.php
- 내용  
	신규주문 버튼 클릭시 실행되는 STEP 1~4 전체 내용

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 제시한 위 내용 그래도 작업 시작해줘


### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 step4_make_lozen_file() / process_orders.php
- 내용  
	로젠형식에 맞춰 데이터를 가져와 작성하는곳
	작성할때 검수를 통해 1,2차 방어를 하는곳

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 이전 업로드한 엑셀로 대화한 내용을 참조
  연락처위험으로 판단한 기준이 몇자기준으로 한지 모르겠지만
  온라인 판매에선 핸드폰 번호를 안심번호로 많이 사용하므로,
 '4자리-4자리-4자리' 형식을 위험에서 제외하기.

  위 변경 후 오늘 작업한 신규주문 17건에 대해서 다시 신규주문 클릭 액션과 같이
  엑셀을 다운로드 하기
  - 내용을 다시한번 확인하기 위함


### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)


==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 getMenuGroups() / app/services/SystemService.php
	 메뉴 간 이동을 위해 필요한 페이지 및 함수 다 참조하기
- 내용  
	메뉴창

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 주문관리 > 미배송 엑셀 재다운
  미배송 엑셀 재다운은 메인페이지 오른쪽 상단에 버튼으로 존재하므로 페이지는 필요없음.
  미배송 엑셀 재다운을 변경
  ['action' => 'inspection', 'label' => '데이터 검수']

  기존 미배송 엑셀 재다운 action => 'lozen_retry'에 해당되는 내용을 위 변경된 내용으로 다 교체

  데이터검수 페이지
  1. orders_raw : raw_json 데이터를 json 파싱해서 표 형식
  2. 각 사이트 table 데이터를 표 형식
  3. 1,2번의 기준은 오늘 신규주문한 데이터 기준
  
  1,2(적용 사이트 다가져와 합친 후 사이트 구분을 줘서 인식할 수 있게)
  내가 시스템상 문제가 없을때까지 검수하기 위한 페이지


### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 getMenuGroups() / app/services/SystemService.php
	 메뉴 간 이동을 위해 필요한 페이지 및 함수 다 참조하기
- 내용  
	메뉴창

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 주문관리 > 미배송 엑셀 재다운
  미배송 엑셀 재다운은 메인페이지 오른쪽 상단에 버튼으로 존재하므로 페이지는 필요없음.
  미배송 엑셀 재다운을 변경
  ['action' => 'inspection', 'label' => '데이터 검수']

  기존 미배송 엑셀 재다운 action => 'lozen_retry'에 해당되는 내용을 위 변경된 내용으로 다 교체

  데이터검수 페이지
  1. orders_raw : raw_json 데이터를 json 파싱해서 표 형식
  2. 각 사이트 table 데이터를 표 형식
  3. 1,2번의 기준은 오늘 신규주문한 데이터 기준
  
  1,2(적용 사이트 다가져와 합친 후 사이트 구분을 줘서 인식할 수 있게)
  내가 시스템상 문제가 없을때까지 검수하기 위한 페이지


### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 inspection() / app/controllers/OrderController.php
	 app/view/order/inspection.php
	 주문관리 > 데이터 검수 페이지에 관련되 페이지 및 함수
- 내용  
	메뉴창

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- orders_raw 테이블 표 구성 형식
  


### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 orders_raw 테이블
- 내용  
	api를 통해서 가져오는 데이터를 담는 테이블

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- orders_raw 에서 order_no에 NVR-HASH-ab9440cb73b69cf38743, NVR-HASH-1a6b977cae89cffa0372
  해당 데이터가 신규 주문이 아닌데 들어가 이유좀 찾아봐줘.

  스마트스토어 기준 14시 30분 이후에 신규주문은 NVR-HASH-720ba5ef9db4c39f37f2
  저 order_no에 해당되는 주문 1개뿐인데 테이블을 확인해보니 위에 2개가 더 들어갔어.

  저 위의 2개의 데이터가 왜 들어갔는지 파악해줘.

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 신규주문 수집기간을 다시 변경.
  이유 : 오늘 신규주문은 가져오지만 오늘 출고한 이후 들어온 주문을 다음날에 인식하지 못할꺼 같다.

적용기준
화~토요일 : 전날 14:30 이후 ~ 오늘 기준
일요일 : 금요일 14:30 이후 ~ 오늘 기준
월요일 : 금요일 14:30 이후 ~ 오늘 기준

위와 같이 다시 변경해줘

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 inspection() / app/controllers/OrderController.php
	 app/view/order/inspection.php
	 주문관리 > 데이터 검수 페이지에 관련되 페이지 및 함수
- 내용  
	데이터 검수페이지로써, 이해 해당되는 데이터를 가져와 검수하는 뷰페이지

### 요구사항 ###

- [기존 기능 유지하며 에러만 수정]
- orders_raw 표 컬럼 변경
  플랫폼, 수취인명 ,수취인전화번호, 상품명(옵션명), 수취인주소, 배송메세지, 주문수량, 주문번호
  플랫폼 제외한 나머지 컬럼은 raw_json에서 가져온다.

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 inspection() / app/controllers/OrderController.php
	 app/view/order/inspection.php
	 주문관리 > 데이터 검수 페이지에 관련되 페이지 및 함수
- 내용  
	데이터 검수페이지로써, 이해 해당되는 데이터를 가져와 검수하는 뷰페이지

### 요구사항 ###

- [기존 기능 유지하며 에러만 수정]
- orders_raw 표
  상품명(옵션명)에서 플랫폼이 쿠팡인 경우에는 sellerProductName(sellerProductItemName)
  raw_json에서 해당 요소로 가져오기.  

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 inspection() / app/controllers/OrderController.php
	 app/view/order/inspection.php
	 주문관리 > 데이터 검수 페이지에 관련되 페이지 및 함수
- 내용  
	데이터 검수페이지로써, 이해 해당되는 데이터를 가져와 검수하는 뷰페이지

### 요구사항 ###

- [기존 기능 유지하며 에러만 수정]
- orders_raw 표
  상품명(옵션명)에서 플랫폼이 smartstore 경우에는 productName(productOption)
  raw_json에서 해당 요소로 가져오기.  

  여기서 productName이건 텍스트가 길기 때문에 한글기준 앞에서 25자만 가져오고 뒤는 잘라줘.

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 inspection() / app/controllers/OrderController.php
	 app/view/order/inspection.php
	 주문관리 > 데이터 검수 페이지에 관련되 페이지 및 함수
- 내용  
	데이터 검수페이지로써, 이해 해당되는 데이터를 가져와 검수하는 뷰페이지

### 요구사항 ###

- [기존 기능 유지하며 에러만 수정]
- 사이트 정규화 테이블 통합 표
  주문번호 - 현재 쿠팡은 유지, 스마트스토어는 상품주문번호 가져오는 내용을 주문번호로 가져오기
  상품주문번호 컬럼은 삭제

  플랫폼, 수취인명, 수취인전화번호, 상품명(옵션명), 수취인주소, 배송메세제, 주문수량, 주문번호

  이렇게 표를 다시 셋팅해줘

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 inspection() / app/controllers/OrderController.php
	 app/view/order/inspection.php
	 주문관리 > 데이터 검수 페이지에 관련되 페이지 및 함수
- 내용  
	데이터 검수페이지로써, 이해 해당되는 데이터를 가져와 검수하는 뷰페이지

### 요구사항 ###

- [기존 기능 유지하며 에러만 수정]
- 사이트 정규화 테이블 통합 표
  해당 변경한 컬럼 순서는 로젠형식에 맞게 한거다.

  그러므로, 로젠형식에 맞춰 작성하는 방식으로 해당표에도 동일하게 작성해줘.
  why? 여기에 로젠형식에 맞게 작성하는 부분을 그대로 가져와서 비교를 해야지.
  여기서 내가 직접 관여해서 이 컬럼은 이거 가져와 이러면 분명 또 오차가 발생할 수 있다.

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 inspection() / app/controllers/OrderController.php
	 app/view/order/inspection.php
	 주문관리 > 데이터 검수 페이지에 관련되 페이지 및 함수
- 내용  
	데이터 검수페이지로써, 이해 해당되는 데이터를 가져와 검수하는 뷰페이지

### 요구사항 ###

- [기존 기능 유지하며 에러만 수정]
- 사이트 정규화 테이블 통합 표
  해당 변경한 컬럼 순서는 로젠형식에 맞게 한거다.

  그러므로, 로젠형식에 맞춰 작성하는 방식으로 해당표에도 동일하게 작성해줘.
  why? 여기에 로젠형식에 맞게 작성하는 부분을 그대로 가져와서 비교를 해야지.
  여기서 내가 직접 관여해서 이 컬럼은 이거 가져와 이러면 분명 또 오차가 발생할 수 있다.

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	  smartstore_order_excel 테이블
- 내용  
	orders_raw raw_json 정규화하여 담는 테이블

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 쿠팡, 스마트스토어 신규주문 범위 수정전 3일전 주문까지 가져오고 했던 내용

  그 상황이였을때, 오늘 신규주문이 아닌 이전 주문까지 가져오면
  그 해당되는 api 데이터를 기존에 있어도 중복으로 orders_raw에 insert 되었는데,
  이후 정규화를 해서 스마트스토어 테이블에 넣을때, 만약 동일 데이터가 있으면 imported_at만 해당 import된 시간으로
  업데이트를 하고 있었어? 그리고 imported_at 뿐만 아니라 다른거까지 업데이트 하는지 확인해줘.

  이 부분도 수정을 해야될꺼 같아.
  update된 내용때문에 오늘 기준 수집데이터로 변경되어 가져오게 된다.
  실제로 데이터를 확인해서 다른 시간관련 컬럼을 보면 보다 이전에 시간으로 찍어서 확인해보니 오늘 데이터가 아니다.


### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	  run_process_orders() / process_orders.php
- 내용  
	신규주문 버튼 클릭 액션시 이뤄지는 전체 함수

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 지금 신규주문 버튼 클릭을 해서 액션이 이뤄지고
  신규주문을 가져왔는데, 쿠팡밖에 없는줄 알고 이후 작업을 진행하려는데
  혹시 몰라 스마트스토어를 확인해 보니 3개의 신규주문이 있다.

  신규주문이 있는데 가져오질 못하는데 이 부분 체크해서 수정해주고
  무엇이 문제였는지 결과 알려줘.


### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

==============================================================================

### 문제 ###
- 발생 위치: [함수명/파일명]
	 process_orders.php
- 내용  
	신규주문 버튼 클릭 액션시 이뤄지는 전체 함수

### 요구사항 ###
- [기존 기능 유지하며 에러만 수정]
- 개발하면서 실제 운영중에 있는 사이트이다.
  계속 개발을 함에 있어 잘되다가도 이상하거나 틀리거나 하는 경우가 많다.

  step 1~4로 만들어놨지만 중간중간 작은 step들이 많다.
  그런 모든걸 생각하고 QA 리스트를 만들려고 한다.

  너가 이에 해당되는 소스 및 전체적인 실행을 판단하고 리스트를 만들어줘.

### 체크 ###
- 수정 요청한 파일, 함수, 함수에 연결된 부분까지만 작업(다른 코드까지 침범하지 않기)
- 코드 작성시 줄바꿈/들어쓰기해서 가독성 있게 작업
- PHP [버전] 호환성 준수
- 한글 깨짐 방지
- 수정 후 php -l로 문법검사까지 해줘
- ship_new_v2 파일만, UTF-8 BOM없음 + CRLF + 탭없음(스페이스4)

